services:
  api:
    build:
      target: dev
    environment:
      APP_ENV: development
      DEV_AUTH_ENABLED: ${DEV_AUTH_ENABLED:-true}
    command:
      - uvicorn
      - gridlens_api_gateway.main:app
      - --host
      - "0.0.0.0"
      - --port
      - "8000"
      - --reload
    volumes:
      - ./services/api-gateway/src:/app/src:ro
      - ${HOME}/.aws:/home/app/.aws:ro
    restart: unless-stopped

  kong:
    restart: unless-stopped

  worker:
    build:
      target: dev
    environment:
      APP_ENV: development
      WORKER_HEARTBEAT_SECONDS: ${WORKER_HEARTBEAT_SECONDS:-10}
    command: ["python", "-m", "gridlens_local_runtime_worker.main"]
    volumes:
      - ./workers/local-runtime-worker/src:/app/src:ro
      - ${HOME}/.aws:/home/app/.aws:ro
    restart: unless-stopped
