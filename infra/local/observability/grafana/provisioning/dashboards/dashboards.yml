apiVersion: 1

providers:
  - name: GridLens
    orgId: 1
    folder: GridLens
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
