# API Gateway Scaffold

This service is a minimal local-runtime placeholder. It exposes `GET /health`
and `GET /healthz` so Docker Compose can prove the API process is alive before
product routes exist.
