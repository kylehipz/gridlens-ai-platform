# GridLens Contracts

Shared public contracts for roles, statuses, API envelopes, pagination, tenant
context, audit actions, events, and jobs.

This package has no service, database, cloud, or frontend dependency. Contract
tests parse the product docs so public role and status vocabulary cannot drift
silently.
